# 📝 แผนการเพิ่มฟีเจอร์แก้ไขวาระและปรับ Layout

**วันที่:** 21 พฤศจิกายน 2568  
**สถานะ:** รอการอนุมัติก่อนแก้ไข

---

## 📋 สรุปความต้องการ

### 1️⃣ เพิ่มฟังก์ชันแก้ไขวาระ
- เปิด Modal แก้ไขเมื่อกดปุ่ม "แก้ไข"
- แก้ไขได้ทุกฟิลด์: ประเภท, หัวข้อ, รายละเอียด, ผู้เสนอ, ไฟล์
- รองรับการลบไฟล์เดิม + อัปโหลดไฟล์ใหม่
- Refresh รายการทันทีหลังบันทึก

### 2️⃣ ปรับ Layout หน้าจัดการวาระ
- ขยายความกว้างตารางให้เต็มหน้าจอ
- ปรับ alignment ปุ่มลบไม่ให้ตกขอบ
- ปรับความกว้างคอลัมน์ให้เหมาะสม

### 3️⃣ ไม่กระทบแท็บอื่น
- แก้เฉพาะแท็บจัดการวาระ
- ไม่แตะ CSS global

---

## 🔍 การวิเคราะห์โค้ดปัจจุบัน

### ✅ Backend - มี API แล้ว
**Endpoint:** `PUT /api/agendas/:id`
- ✅ มีอยู่แล้วที่ `backend/src/server.js` บรรทัด 791
- ✅ รองรับ authentication (secretary or manager)
- ✅ รองรับการ update ทุกฟิลด์
- ⚠️ **ยังไม่รองรับ multipart/form-data** (ไฟล์หลายไฟล์)

### ⚠️ Frontend - ยังไม่มี
- ❌ ไม่มี `updateAgenda` function ใน `api.js`
- ❌ ไม่มี Edit Modal component
- ❌ ไม่มีปุ่ม "แก้ไข" ใน `AgendasManager.jsx`

### 📊 ตาราง agenda_files
- ✅ มีตาราง `agenda_files` สำหรับเก็บไฟล์หลายไฟล์
- ✅ มี foreign key `agenda_id`

---

## 🎯 แผนการแก้ไข


### 🔧 Part 1: Backend - เพิ่ม Endpoint สำหรับ Update พร้อมไฟล์

**ไฟล์:** `backend/src/server.js`

**เพิ่ม Endpoint ใหม่:**
```javascript
// Update agenda with files (secretary or manager only)
app.put('/api/agendas/:id/with-files', 
  authenticateToken, 
  requireSecretaryOrManager, 
  upload.array('files', 5), 
  async (req, res) => {
    // 1. Update agenda data
    // 2. Delete old files if requested
    // 3. Upload new files to agenda_files table
    // 4. Return updated agenda with files
  }
);
```

**Logic:**
1. รับ `deleteFileIds` จาก body (array ของ file IDs ที่ต้องการลบ)
2. ลบไฟล์เก่าจาก `agenda_files` และ filesystem
3. อัปโหลดไฟล์ใหม่ (ถ้ามี)
4. Update ข้อมูลวาระใน `meeting_agendas`

---

### 🎨 Part 2: Frontend - เพิ่ม API Function

**ไฟล์:** `frontend/src/services/api.js`

**เพิ่ม 2 functions:**
```javascript
// Get agenda with files
export const getAgendaWithFiles = async (id) => {
  const response = await api.get(`/agendas/${id}`);
  return response.data;
};

// Update agenda with files
export const updateAgendaWithFiles = async (id, formData) => {
  const response = await api.put(`/agendas/${id}/with-files`, formData, {
    headers: { 'Content-Type': 'multipart/form-data' },
    timeout: 60000
  });
  return response.data;
};
```

---

### 📝 Part 3: สร้าง Edit Modal Component

**ไฟล์ใหม่:** `frontend/src/components/management/EditAgendaModal.jsx`

**Features:**
- แสดงข้อมูลวาระปัจจุบัน
- แก้ไขทุกฟิลด์ (ประเภท, หัวข้อ, รายละเอียด, ผู้เสนอ)
- แสดงรายการไฟล์เดิม พร้อมปุ่มลบ
- อัปโหลดไฟล์ใหม่ (MultipleFileUpload component)
- ปุ่มบันทึก + ยกเลิก

**Structure:**
```jsx
<div className="modal-overlay">
  <div className="modal-content">
    <h2>✏️ แก้ไขวาระการประชุม</h2>
    <form>
      {/* Form fields */}
      {/* Existing files list with delete buttons */}
      {/* New files upload */}
      <div className="modal-actions">
        <button type="button" onClick={onCancel}>ยกเลิก</button>
        <button type="submit">💾 บันทึก</button>
      </div>
    </form>
  </div>
</div>
```


---

### 🔄 Part 4: แก้ไข AgendasManager.jsx

**ไฟล์:** `frontend/src/components/management/AgendasManager.jsx`

**เพิ่ม:**
1. State สำหรับ Edit Modal
```javascript
const [showEditModal, setShowEditModal] = useState(false);
const [editTarget, setEditTarget] = useState(null);
```

2. Function เปิด Modal
```javascript
const handleEdit = async (agenda) => {
  // Load full agenda data with files
  const fullData = await getAgendaWithFiles(agenda.id);
  setEditTarget(fullData.data);
  setShowEditModal(true);
};
```

3. เพิ่มปุ่ม "แก้ไข" ในตาราง
```jsx
<td>
  <button onClick={() => handleEdit(agenda)} className="btn-icon btn-edit">
    ✏️
  </button>
  <button onClick={() => handleDelete(agenda.id)} className="btn-icon btn-danger">
    🗑️
  </button>
</td>
```

4. เพิ่ม Modal component
```jsx
{showEditModal && (
  <EditAgendaModal
    agenda={editTarget}
    onSuccess={() => {
      setShowEditModal(false);
      loadAgendas();
    }}
    onCancel={() => setShowEditModal(false)}
  />
)}
```

---

### 🎨 Part 5: ปรับ Layout ตาราง

**ไฟล์:** `frontend/src/components/management/AgendasManager.jsx`

**แก้ไข CSS ใน styled-jsx:**

```css
/* เพิ่มความกว้างตาราง */
.agendas-manager {
  max-width: 100%; /* เดิมอาจจำกัดไว้ */
  width: 100%;
}

.table-container {
  overflow-x: auto;
  width: 100%;
}

table {
  width: 100%;
  min-width: 1000px; /* ป้องกันตารางแคบเกินไป */
}

/* ปรับความกว้างคอลัมน์ */
th:nth-child(1) { width: 50px; }   /* Checkbox */
th:nth-child(2) { width: 120px; }  /* เลขที่การประชุม */
th:nth-child(3) { width: 80px; }   /* วาระที่ */
th:nth-child(4) { width: auto; }   /* หัวข้อ - ให้กว้างที่สุด */
th:nth-child(5) { width: 100px; }  /* ประเภท */
th:nth-child(6) { width: 150px; }  /* กลุ่มงาน */
th:nth-child(7) { width: 120px; }  /* วันที่ */
th:nth-child(8) { width: 100px; }  /* จัดการ */

/* ปรับ alignment ปุ่มจัดการ */
td:last-child {
  text-align: center;
  white-space: nowrap;
}

.btn-icon {
  margin: 0 3px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

/* ปรับหัวข้อไม่ให้ตัดมากเกินไป */
.agenda-topic {
  max-width: none; /* ลบข้อจำกัดเดิม */
  overflow: visible;
  text-overflow: clip;
  white-space: normal;
  word-break: break-word;
}
```


---

## 📊 สรุปไฟล์ที่ต้องสร้าง/แก้ไข

| ลำดับ | ไฟล์ | การดำเนินการ | เหตุผล |
|------|------|-------------|--------|
| 1 | `backend/src/server.js` | เพิ่ม endpoint `PUT /api/agendas/:id/with-files` | รองรับการ update พร้อมไฟล์ |
| 2 | `frontend/src/services/api.js` | เพิ่ม `getAgendaWithFiles()` และ `updateAgendaWithFiles()` | API functions สำหรับ frontend |
| 3 | `frontend/src/components/management/EditAgendaModal.jsx` | **สร้างใหม่** | Modal สำหรับแก้ไขวาระ |
| 4 | `frontend/src/components/management/AgendasManager.jsx` | แก้ไข - เพิ่มปุ่มแก้ไข + ปรับ layout | เพิ่มฟีเจอร์แก้ไข + ปรับ UI |

**รวม:** 3 ไฟล์แก้ไข + 1 ไฟล์สร้างใหม่

---

## 🎯 Flow การทำงาน

### 📝 Edit Flow
```
1. User กดปุ่ม "✏️ แก้ไข" ในตาราง
   ↓
2. เรียก getAgendaWithFiles(id) → ดึงข้อมูลวาระ + ไฟล์
   ↓
3. เปิด EditAgendaModal พร้อมข้อมูล
   ↓
4. User แก้ไขข้อมูล:
   - แก้ไขฟิลด์ต่าง ๆ
   - ลบไฟล์เดิม (เก็บ ID ไว้)
   - อัปโหลดไฟล์ใหม่
   ↓
5. กดบันทึก → ส่ง FormData ไป PUT /api/agendas/:id/with-files
   ↓
6. Backend:
   - Update ข้อมูลวาระ
   - ลบไฟล์ที่ระบุ (จาก agenda_files + filesystem)
   - บันทึกไฟล์ใหม่
   ↓
7. Frontend: ปิด Modal + Refresh รายการ
```

---

## 🔒 Security & Validation

### Backend
- ✅ Authentication: `authenticateToken`
- ✅ Authorization: `requireSecretaryOrManager`
- ✅ File validation: multer filter (ตามที่มีอยู่แล้ว)
- ✅ Audit log: บันทึกการแก้ไข

### Frontend
- ✅ Validate required fields
- ✅ File size limit (20MB/file)
- ✅ File type validation
- ✅ Confirm before delete files

---

## 🎨 UI/UX Improvements

### ปรับ Layout
1. **ตารางกว้างขึ้น** - ใช้พื้นที่เต็มหน้าจอ
2. **คอลัมน์หัวข้อ** - ขยายให้แสดงข้อความได้มากขึ้น
3. **ปุ่มจัดการ** - จัดกลาง ไม่ตกขอบ
4. **Responsive** - ทำงานได้ทั้ง desktop และ mobile

### Modal Design
- ขนาดใหญ่พอสำหรับฟอร์ม
- Scrollable content
- ปิดได้ด้วย overlay click หรือปุ่มยกเลิก
- Loading state ขณะบันทึก

---

## ⚠️ ข้อควรระวัง

1. **ไม่แก้ไข Global CSS** - ใช้ styled-jsx เฉพาะ component
2. **ไม่กระทบแท็บอื่น** - แก้เฉพาะ AgendasManager
3. **ทดสอบการลบไฟล์** - ต้องลบทั้ง DB และ filesystem
4. **Handle errors** - แสดง error message ที่เข้าใจง่าย
5. **Refresh data** - ต้อง reload หลังแก้ไขสำเร็จ

---

## 🧪 Test Cases

### Test 1: แก้ไขข้อมูลวาระ (ไม่แตะไฟล์)
- แก้ไขหัวข้อ, ประเภท, รายละเอียด
- บันทึก → ตรวจสอบข้อมูลอัปเดต

### Test 2: ลบไฟล์เดิม
- เปิด edit modal
- ลบไฟล์เดิม 1-2 ไฟล์
- บันทึก → ตรวจสอบไฟล์ถูกลบ

### Test 3: เพิ่มไฟล์ใหม่
- เปิด edit modal
- อัปโหลดไฟล์ใหม่ 2-3 ไฟล์
- บันทึก → ตรวจสอบไฟล์ถูกเพิ่ม

### Test 4: ลบไฟล์เดิม + เพิ่มไฟล์ใหม่
- ลบไฟล์เดิม 1 ไฟล์
- เพิ่มไฟล์ใหม่ 2 ไฟล์
- บันทึก → ตรวจสอบทั้งสองอย่าง

### Test 5: Layout Responsive
- ทดสอบบน desktop (1920px)
- ทดสอบบน tablet (768px)
- ทดสอบบน mobile (375px)

---

## 📝 ขั้นตอนถัดไป

**รอการอนุมัติจากผู้ใช้:**
1. ✅ ตรวจสอบแผนการแก้ไขว่าถูกต้องหรือไม่
2. ✅ อนุมัติแผนการแก้ไข
3. ⏳ รอคำสั่ง **"แก้ไขโค้ด"** หรือ **"ทบทวนใหม่"**

---

**หมายเหตุ:** เอกสารนี้เป็นการวางแผนเท่านั้น ยังไม่ได้แก้ไขโค้ดใด ๆ
