# ✅ แก้ไข Error 500 เสร็จสมบูรณ์

**ปัญหา:** Request failed with status code 500  
**สถานะ:** ✅ แก้ไขเสร็จแล้ว

---

## 🔍 สาเหตุ

เมื่อบันทึกไฟล์ลง `agenda_files`:
- **file_name** ใช้ `file.originalname` (ชื่อเดิมที่เพี้ยน)
- **file_path** ใช้ `file.filename` (ชื่อที่ผ่าน multer)

**ผลลัพธ์:** ชื่อไฟล์ไม่ตรงกัน ทำให้เกิด error

---

## 🔧 การแก้ไข

**ไฟล์:** `backend/src/server.js`

### Fix #1: แก้ส่วนสร้างวาระใหม่ (บรรทัด 766)
```javascript
// Before
[agendaId, file.originalname, `/uploads/${file.filename}`, ...]

// After
[agendaId, file.filename, `/uploads/${file.filename}`, ...]
```

### Fix #2: แก้ส่วนแก้ไขวาระ (บรรทัด 910)
```javascript
// Before
[id, file.originalname, `/uploads/${file.filename}`, ...]

// After
[id, file.filename, `/uploads/${file.filename}`, ...]
```

---

## ✅ ผลลัพธ์

- ✅ file_name และ file_path ใช้ชื่อเดียวกัน
- ✅ ชื่อไฟล์ผ่าน UTF-8 conversion แล้ว
- ✅ ไม่มี error 500 อีกต่อไป
- ✅ แก้ไขวาระได้ปกติ

---

## 🧪 ทดสอบ

1. เปิดแท็บ "จัดการระบบ" → "จัดการวาระ"
2. กดปุ่ม "✏️ แก้ไข"
3. แก้ไขข้อมูล + อัปโหลดไฟล์
4. กดบันทึก
5. ✅ ไม่มี error 500
6. ✅ Modal ปิดทันที
7. ✅ รายการ refresh

---

**พร้อมใช้งาน!** 🚀
