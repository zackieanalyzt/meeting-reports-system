# 🚀 START HERE - Authentication System

## ยินดีต้อนรับสู่ระบบ Authentication!

ระบบ Authentication และ Role-Based Access Control ได้รับการพัฒนาเสร็จสมบูรณ์แล้ว ✅

---

## 📋 สิ่งที่คุณต้องทำ (3 ขั้นตอน)

### ขั้นตอนที่ 1: Setup Database (5 นาที)

เปิด pgAdmin หรือ DBeaver แล้วรัน SQL files:

1. **auth-schema.sql** - สร้างตาราง users และ audit_logs
   ```
   ไฟล์: database/auth-schema.sql
   ```

2. **sample-users.sql** - เพิ่มผู้ใช้ตัวอย่าง (แก้ไข username ให้ตรงกับข้อมูลจริง)
   ```
   ไฟล์: database/sample-users.sql
   ```

หรือใช้ command line:
```bash
psql -h 192.168.100.70 -p 5432 -U postgres -d meeting_mgmt -f database/auth-schema.sql
psql -h 192.168.100.70 -p 5432 -U postgres -d meeting_mgmt -f database/sample-users.sql
```

---

### ขั้นตอนที่ 2: เพิ่มผู้ใช้ที่มีสิทธิพิเศษ (2 นาที)

เปิด pgAdmin/DBeaver และรัน SQL:

```sql
-- เพิ่มเจ้าหน้าที่ธุรการ (แทนที่ 'your_username' ด้วย username จริง)
INSERT INTO users (username, role, is_active) 
VALUES ('your_username', 'secretary', true)
ON CONFLICT (username) DO UPDATE SET role = 'secretary';

-- เพิ่มหัวหน้ากลุ่มงาน (ถ้ามี)
INSERT INTO users (username, role, is_active) 
VALUES ('manager_username', 'manager', true)
ON CONFLICT (username) DO UPDATE SET role = 'manager';
```

**สำคัญ:** Username ต้องตรงกับที่มีในตาราง `personnel` ของ MariaDB

---

### ขั้นตอนที่ 3: เริ่มต้นใช้งาน (1 นาที)

เปิด 2 terminals:

**Terminal 1 - Backend:**
```bash
cd backend
npm start
```

รอจนเห็น:
```
🚀 Backend server running on port 3001
✅ MariaDB connected successfully
```

**Terminal 2 - Frontend:**
```bash
cd frontend
npm run dev
```

เปิดเบราว์เซอร์: http://localhost:5173

---

## 🎯 ทดสอบระบบ

### 1. Login
- ใช้ username/password จากระบบ HR (ตาราง personnel)
- ถ้า login สำเร็จ จะเห็นชื่อและ role badge ที่มุมขวาบน

### 2. ตรวจสอบ Role
- **Secretary** (เจ้าหน้าที่ธุรการ): เห็นปุ่ม + ทุกแท็บ
- **Manager** (หัวหน้ากลุ่มงาน): เห็นปุ่ม + เฉพาะแท็บวาระ
- **User** (ผู้ใช้ทั่วไป): ไม่เห็นปุ่ม + เลย

### 3. ตรวจสอบ Audit Logs
```sql
SELECT * FROM audit_logs 
ORDER BY created_at DESC 
LIMIT 20;
```

---

## 📚 เอกสารที่ควรอ่าน

### เริ่มต้นใช้งาน
1. **QUICK_START_AUTH.md** ⭐ - เริ่มต้นใน 5 นาที (อ่านก่อน!)
2. **AUTHENTICATION_SETUP.md** - คู่มือการติดตั้งแบบละเอียด

### สำหรับ Developer
3. **API_AUTH_DOCUMENTATION.md** - API endpoints และ permissions
4. **TEST_SCENARIOS.md** - วิธีทดสอบระบบ
5. **PHASE2A_IMPLEMENTATION_SUMMARY.md** - สรุปการพัฒนา

### สรุปทั้งหมด
6. **AUTHENTICATION_COMPLETE.md** - สรุปทุกอย่างในที่เดียว
7. **PROMPT11_SUMMARY.md** - สรุปการพัฒนา Prompt 11

---

## 🎨 Role Permissions

| Feature | Secretary | Manager | User |
|---------|-----------|---------|------|
| ดูการประชุม | ✅ | ✅ | ✅ |
| สร้าง/แก้ไข/ลบ การประชุม | ✅ | ❌ | ❌ |
| ดูวาระ | ✅ | ✅ | ✅ |
| สร้าง/แก้ไข/ลบ วาระ | ✅ | ✅ | ❌ |
| ดูรายงาน | ✅ | ✅ | ✅ |
| อัพโหลดรายงาน | ✅ | ❌ | ❌ |

---

## ❓ ปัญหาที่พบบ่อย

### Q: ไม่สามารถ login ได้
**A:** ตรวจสอบ:
1. MariaDB เชื่อมต่อได้หรือไม่?
2. Username/Password ถูกต้องหรือไม่?
3. Backend console มี error หรือไม่?

### Q: Login ได้แต่ไม่มีสิทธิ์
**A:** ตรวจสอบ role:
```sql
SELECT username, role, is_active FROM users WHERE username = 'your_username';
```
ถ้าไม่มีข้อมูล = role เป็น 'user' (ผู้ใช้ทั่วไป)

### Q: Token หมดอายุ
**A:** Token มีอายุ 24 ชั่วโมง - กด "ออกจากระบบ" แล้ว login ใหม่

---

## 🔍 ตรวจสอบว่าทุกอย่างทำงาน

### Backend
```bash
curl http://localhost:3001/api/health
```
ควรได้: `{"success":true,"message":"API is running..."}`

### Database
```sql
-- ตรวจสอบตาราง users
SELECT COUNT(*) FROM users;

-- ตรวจสอบตาราง audit_logs
SELECT COUNT(*) FROM audit_logs;
```

### Frontend
เปิด http://localhost:5173 ควรเห็นหน้า login

---

## 📞 ต้องการความช่วยเหลือ?

1. อ่าน **QUICK_START_AUTH.md** ก่อน
2. ตรวจสอบ backend/frontend console logs
3. ดู audit_logs table
4. อ่าน **AUTHENTICATION_SETUP.md** สำหรับรายละเอียด

---

## ✅ Checklist

- [ ] รัน auth-schema.sql แล้ว
- [ ] รัน sample-users.sql แล้ว (แก้ไข username)
- [ ] เพิ่มผู้ใช้ที่มีสิทธิพิเศษแล้ว
- [ ] Backend ทำงานแล้ว (port 3001)
- [ ] Frontend ทำงานแล้ว (port 5173)
- [ ] Login ได้แล้ว
- [ ] ตรวจสอบ role แล้ว
- [ ] ทดสอบ permissions แล้ว
- [ ] ตรวจสอบ audit logs แล้ว

---

## 🎉 เสร็จแล้ว!

ถ้าทำครบทุกขั้นตอน ระบบ Authentication พร้อมใช้งานแล้ว! 🚀

**Happy Coding!** 💻

---

<div align="center">

**Need more help?**  
Read: [QUICK_START_AUTH.md](./QUICK_START_AUTH.md)

**Want details?**  
Read: [AUTHENTICATION_COMPLETE.md](./AUTHENTICATION_COMPLETE.md)

</div>
