# 🔐 Authentication System Setup Guide

## ขั้นตอนการติดตั้งระบบ Authentication

### 1. ติดตั้ง Dependencies

Backend dependencies ได้ถูกติดตั้งแล้ว:
- jsonwebtoken (JWT authentication)
- bcryptjs (Password hashing)
- mysql2 (MariaDB connection)

Frontend dependencies ได้ถูกติดตั้งแล้ว:
- react-router-dom (Routing)

### 2. ตั้งค่า Database Schema

รัน SQL script เพื่อสร้างตารางที่จำเป็น:

```bash
# เชื่อมต่อ PostgreSQL และรัน auth-schema.sql
psql -h 192.168.100.70 -p 5432 -U postgres -d meeting_mgmt -f database/auth-schema.sql
```

หรือใช้ pgAdmin/DBeaver เปิดไฟล์ `database/auth-schema.sql` และรัน

Schema นี้จะสร้าง:
- ตาราง `users` - เก็บ role และสิทธิพิเศษ
- ตาราง `audit_logs` - เก็บสถิติการใช้งาน
- เพิ่มคอลัมน์ `created_by`, `updated_by` ในตารางที่มีอยู่

### 3. ตรวจสอบ Environment Variables

ไฟล์ `backend/.env` ได้ถูกอัพเดทแล้วด้วย:

```env
# MariaDB Configuration for Authentication
MARIADB_HOST=192.168.100.170
MARIADB_PORT=3306
MARIADB_DATABASE=hr
MARIADB_USER=root
MARIADB_PASSWORD=cjv671

# JWT Configuration
JWT_SECRET=meeting_mgmt_secret_key_2025_lamphun_pho
JWT_EXPIRES_IN=24h
```

### 4. เพิ่มผู้ใช้ที่มีสิทธิพิเศษ

เข้าไปที่ PostgreSQL และเพิ่มผู้ใช้ที่ต้องการให้มีสิทธิพิเศษ:

```sql
-- เพิ่มเจ้าหน้าที่ธุรการ (secretary)
INSERT INTO users (username, role, is_active) 
VALUES ('admin_username', 'secretary', true)
ON CONFLICT (username) DO UPDATE SET role = 'secretary';

-- เพิ่มหัวหน้ากลุ่มงาน (manager)
INSERT INTO users (username, role, is_active) 
VALUES ('manager_username', 'manager', true)
ON CONFLICT (username) DO UPDATE SET role = 'manager';
```

**หมายเหตุ:** ผู้ใช้ที่ไม่มีในตาราง `users` จะได้รับ role เป็น `user` (ผู้ใช้ทั่วไป) โดยอัตโนมัติ

### 5. ทดสอบการเชื่อมต่อ MariaDB

ตรวจสอบว่าสามารถเชื่อมต่อกับ MariaDB HR database ได้:

```bash
mysql -h 192.168.100.170 -u root -p hr
# Password: cjv671
```

ตรวจสอบตาราง personnel:
```sql
SELECT username, prefix, fname, lname FROM personnel LIMIT 5;
```

### 6. เริ่มต้นใช้งาน

```bash
# เริ่ม Backend
cd backend
npm start

# เริ่ม Frontend (terminal ใหม่)
cd frontend
npm run dev
```

### 7. ทดสอบระบบ

1. เปิดเบราว์เซอร์ไปที่ `http://localhost:5173`
2. ระบบจะ redirect ไปหน้า login อัตโนมัติ
3. ใช้ username/password จากตาราง `personnel` ใน MariaDB
4. ตรวจสอบ role และสิทธิ์การใช้งาน

## 🎯 Role-Based Permissions

### Secretary (เจ้าหน้าที่ธุรการ)
- ✅ สร้าง/แก้ไข/ลบ การประชุม
- ✅ สร้าง/แก้ไข/ลบ วาระการประชุม
- ✅ อัพโหลด/จัดการ รายงานการประชุม

### Manager (หัวหน้ากลุ่มงาน)
- ❌ ดูการประชุมอย่างเดียว
- ✅ สร้าง/แก้ไข/ลบ วาระการประชุม
- ❌ ดูรายงานอย่างเดียว

### User (ผู้ใช้ทั่วไป)
- ❌ ดูการประชุมอย่างเดียว
- ❌ ดูวาระอย่างเดียว
- ❌ ดูรายงานอย่างเดียว

## 📊 Audit Logging

ระบบจะบันทึกการกระทำต่อไปนี้ใน `audit_logs`:

- ✅ Login/Logout
- ✅ การดูรายงาน/วาระ
- ✅ การดาวน์โหลดไฟล์
- ✅ การสร้าง/แก้ไข/ลบข้อมูล

ตรวจสอบ audit logs:
```sql
SELECT * FROM audit_logs 
ORDER BY created_at DESC 
LIMIT 50;
```

## 🔧 Troubleshooting

### ปัญหา: ไม่สามารถ login ได้
- ตรวจสอบว่า MariaDB เชื่อมต่อได้
- ตรวจสอบ username/password ในตาราง personnel
- ตรวจสอบว่า password ถูก hash ด้วย MD5

### ปัญหา: Token หมดอายุ
- Token มีอายุ 24 ชั่วโมง (ตั้งค่าใน JWT_EXPIRES_IN)
- Login ใหม่เพื่อรับ token ใหม่

### ปัญหา: ไม่มีสิทธิ์ใช้งาน
- ตรวจสอบ role ในตาราง users
- ตรวจสอบว่า is_active = true

## 📝 API Endpoints

### Authentication
- `POST /api/auth/login` - เข้าสู่ระบบ
- `POST /api/auth/logout` - ออกจากระบบ
- `GET /api/auth/verify` - ตรวจสอบ token

### Protected Endpoints
ทุก endpoint ต้องส่ง Authorization header:
```
Authorization: Bearer <token>
```

## 🚀 Next Steps (Phase 2B)

- [ ] Enhanced audit logging
- [ ] View/Download statistics dashboard
- [ ] Multiple file upload support
- [ ] User management interface
- [ ] Role assignment UI
