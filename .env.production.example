# ========================================
# Meeting Management System
# Production Environment Variables
# ========================================

# ========================================
# PostgreSQL Database (Primary)
# ========================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=meeting_mgmt
DB_USER=postgres
DB_PASS=your_secure_password_here

# ========================================
# MariaDB Database (Authentication)
# ========================================
MARIADB_HOST=localhost
MARIADB_PORT=3306
MARIADB_DATABASE=hr
MARIADB_USER=root
MARIADB_PASSWORD=your_secure_password_here

# ========================================
# Backend Server
# ========================================
PORT=3001
NODE_ENV=production
UPLOADS_PATH=/app/uploads

# ========================================
# JWT Configuration
# ========================================
# IMPORTANT: Generate a random secret key (min 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=change_this_to_random_secret_min_32_chars
JWT_EXPIRES_IN=24h

# ========================================
# Frontend
# ========================================
# Option 1: Use dynamic detection (recommended)
# Leave empty to use window.location.hostname
VITE_API_URL=

# Option 2: Set specific URL
# VITE_API_URL=http://your_server_ip:3001/api

# ========================================
# pgAdmin (Optional)
# ========================================
PGADMIN_EMAIL=admin@meeting.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ========================================
# Docker Compose
# ========================================
FRONTEND_PORT=3000

# ========================================
# Security Notes
# ========================================
# 1. NEVER commit this file with real passwords
# 2. Use strong passwords (min 16 characters)
# 3. Generate random JWT_SECRET: openssl rand -base64 32
# 4. Change default pgAdmin credentials
# 5. Use environment-specific .env files
# 6. Keep .env files in .gitignore

# ========================================
# How to Use
# ========================================
# 1. Copy this file:
#    cp .env.production.example backend/.env
#
# 2. Edit with your values:
#    nano backend/.env
#
# 3. Generate JWT secret:
#    openssl rand -base64 32
#
# 4. Test connection:
#    psql -h localhost -U postgres -d meeting_mgmt
#
# 5. Deploy:
#    docker-compose -f docker-compose.production.yml up -d
